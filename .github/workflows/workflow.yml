name: Code Style Checks & Tests
on:
  push:
    branches:
      - master
      - dev
  pull_request:

jobs:
  check:
    name: Check
    runs-on: cr.yandex/crpabbati0r6r7i5ee8c/lizaalert_backend-ci:latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Flake8 check
        run: poetry run flake8 .

      - name: Black check
        run: poetry run black --check .

      - name: Isort check
        run: poetry run isort --check-only --skip-gitignore .

  test:
    name: Test
    runs-on: cr.yandex/crpabbati0r6r7i5ee8c/lizaalert_backend-ci:latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Build and run app
        run: |
          cp lizaalert-backend/env.sample lizaalert-backend/.env
          cp services/postgres/env.sample services/postgres/.env
          docker-compose up -d
          sleep 20

      - name: Run tests
        run: docker-compose exec -T backend pytest tests/
