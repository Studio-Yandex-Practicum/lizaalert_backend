[tool.poetry]
name = "lizaalert-backend"
version = "0.1.0"
description = ""
authors = ["Pavel Melnikov <room.apps@mail.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
